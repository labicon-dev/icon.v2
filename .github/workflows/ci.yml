name: CI

on:
  push:
  pull_request:

jobs:
  lint-and-build:
    name: Lint & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Versão vem do campo `packageManager` do package.json — mantém CI e
      # máquina local no mesmo pnpm.
      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint (oxlint)
        run: pnpm run lint

      - name: Check formatting (Prettier)
        run: pnpm run format:check

      - name: Test (Vitest)
        run: pnpm run test

      - name: Build
        run: pnpm run build
        env:
          VITE_API_BASE_URL: https://example.invalid
          VITE_MEMBER_FETCH_TOKEN: ci-placeholder
